// import initialState from "../data/data";

const initalState = [];

const Products = (state=initalState,action) => {
	switch (action.type) {
		default:
			return state;
	}
}
export default Products;